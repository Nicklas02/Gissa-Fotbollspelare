package model;

/**
 * Poängen med denna enum-klassen är att lagra de olika "hårdkodade" värden på ett ställe, vilket
 * förebygger buggar.
 */
public enum Values {
    SampleSize,
    QuestionSetSize,
}
